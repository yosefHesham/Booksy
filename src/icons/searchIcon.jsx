import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const SearchIcon = ({ className }) => (
  <FontAwesomeIcon className={className} icon="fas fa-search" />
);
export default SearchIcon;
